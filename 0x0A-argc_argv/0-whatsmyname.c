#include "main.h"
#include <stdio.h>
/**
 * main - prints the name
 *@argc: number of arguments
 *@argv: the array
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	int i = 0;


	if (i == 0)
	{	void(argc);
		printf("%s\n", argv[i]);
	}
	return (0);
}
